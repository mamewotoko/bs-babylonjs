{
    "name": "bs-babylonjs",
    "version": "0.0.1",
    "scripts": {
        "clean": "bsb -clean-world",
        "watch": "bsb -make-world -w",
        "build": "bsb -make-world",
        "doc-init": "yarn run bsdoc support-files",
        "serve": "http-server",
        "doc": "mkdir -p docs && yarn run bsdoc build -o docs bs-three"
    },
    "devDependencies": {
        "webpack-cli": "^4.9.1",
        "babelify": "^10.0.0",
        "babylonjs": "^4.2.0",
        "browserify": "^17.0.0",
        "bs-platform": "^9.0.2",
        "bs-promise-monad": "^0.4.4",
        "bs-webapi": "^0.19.1",
        "http-server": "^14.0.0"
    }
}
